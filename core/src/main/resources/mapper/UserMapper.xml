<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiblog.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wiblog.entity.User">
            <id column="uid" property="uid"/>
            <result column="phone" property="phone"/>
            <result column="username" property="username"/>
            <result column="password" property="password"/>
            <result column="sex" property="sex"/>
            <result column="email" property="email"/>
            <result column="avatar_img" property="avatarImg"/>
            <result column="create_time" property="createTime"/>
            <result column="logged" property="logged"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
uid, phone, username, password, sex, email, avatar_img, create_time, logged
    </sql>

    <select id="login" parameterType="com.wiblog.entity.User" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        <where>
            <if test="username != null">
                username = #{username}
            </if>
            <if test="phone != null">
                phone = #{phone}
            </if>
            <if test="email != null">
                email = #{email}
            </if>
            and password = #{password}
        </where>
    </select>
    <select id="checkUsername" parameterType="string" resultType="java.lang.Integer">
        SELECT
        COUNT(*)
        FROM user
        WHERE username=#{username}
    </select>
    <select id="checkPhone" parameterType="string" resultType="java.lang.Integer">
        SELECT
        COUNT(*)
        FROM user
        WHERE phone=#{phone}
    </select>
    <select id="checkEmail" parameterType="string" resultType="java.lang.Integer">
        SELECT
        COUNT(*)
        FROM user
        WHERE email=#{email}
    </select>

    <select id="selectUsername" resultType="map">
        select username as value from user
    </select>

    <select id="selectUserManagePage" resultType="com.wiblog.entity.User">
        select
        <include refid="Base_Column_List"/>
        from user
        <where>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="username != null and username != ''">
                and username like concat('%',#{username},'%')
            </if>
        </where>
    </select>
</mapper>